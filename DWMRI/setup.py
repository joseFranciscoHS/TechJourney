#!/usr/bin/env python3
"""Backward-compatible entrypoint; configuration lives in pyproject.toml."""

from setuptools import setup  # pyright: ignore[reportMissingModuleSource]

if __name__ == "__main__":
    setup()
